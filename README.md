🎯 Назначение устройства

Создание иллюзии «замершего» или «вращающегося» колесного диска с помощью LED-ленты, синхронизированной с импульсами ABS-датчика.

📟 1. Аппаратные компоненты

ESP32-C3 SuperMini

OLED дисплей SH1106 (I²C)

Энкодер с кнопкой

Вход ABS-датчика

LED-лента (управление по цифровому пину)

EEPROM (встроенная NVS → используем Preferences)

🧩 2. Принцип работы

Контроллер принимает импульсы от ABS-датчика.

Автоматически определяет:

количество импульсов ABS за один оборот колеса (IMP/REV).

Пользователь задаёт:

количество спиц реального диска (3…12)

фазу (смещение) для выравнивания эффекта (–5…+5)

яркость (0…10)

На основе этих параметров ESP включает LED-ленту в строго нужный момент, создавая эффект:

полного замирания колеса

легкого вращения вперёд / назад (смещением фазы)

🖥 3. Главный экран

Отображает всю ключевую информацию:

IMP/REV: 48
SPOKES :  6
PHASE  : +2
BRIGHT :  7

CLICK = MENU


Обновляется в реальном времени.

⚙️ 4. Управление
Вращение энкодера:

изменение параметров

перемещение по меню

Короткое нажатие:

вход/выбор в меню

подтверждение пункта

Долгое нажатие (2 секунды) в рабочем режиме:

сброс фазы в 0

📑 5. Структура меню
> Spokes       (3..12)
  Phase        (-5..+5)
  Brightness   (0..10)
  Reset
  Exit

Пункт RESET:
> Reset phase
  Reset brightness
  Reset spokes
  Reset IMP/REV
  Reset ALL

💾 6. Хранение данных (EEPROM / NVS)

Сохраняем:

IMP/REV (автоматически определённое)

Phase

Brightness

Spokes

Загружаются при старте.

🔄 7. Автоопределение количества импульсов ABS (IMP/REV)
Логика:

Приходит серия импульсов → фиксируем.

Длинная пауза (>150 мс) = «конец оборота».

Количество импульсов фиксируется как кандидат.

Фильтрация:

значение стабильно 2 раза подряд

нет шумов

После стабилизации записываем в EEPROM.

Отображается на главном экране.

🌟 8. Параметры LED-синхронизации

Используем:

IMP/REV

SPOKES

PHASE

Формула для вычисления момента свечения:

step = IMP/REV / SPOKES     // количество импульсов на одну спицу
trigger_index = phase + n*step


При совпадении текущего счетчика импульсов → включаем LED.
